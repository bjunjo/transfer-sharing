<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Transfer Sharing</title>
  <link  rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.1.4/semantic.min.css">
  <link  rel="stylesheet" type="text/css" href="/stylesheets/app.css">
</head>
<body>
<div class="ui fixed menu">
  <div class="ui container">
      <a href="/stories" class="item"><i class="code icon"></i>TS</a>
  </div>
  <% if(!currentUser){ %>
    <a href="/register" class="item">Write a story</a>
    <a href="/login" class="item">Log in</a>
    <a href="/register" class="item">Sign up</a>
  <% } else {  %>
    <a href="/stories" class="item">Signed In As <%= currentUser.username %> </a>
    <a href="/stories/new" class="item">Write a story</a>
    <a href="/logout" class="item">Log out</a>
  <% } %>
</div>
<div id="content" class="ui container">
  <% if(error && error.length > 0){ %> 
      <div class="ui warning message">
        <div class="header">
          <%= error  %>
        </div>
      </div>
    <% } %> 
    <% if(success && success.length > 0){ %>
        <div class="ui success message">
          <div class="header">
            <%= success  %>
          </div>
       </div>
  <% } %>
</div>